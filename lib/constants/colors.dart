import 'package:flutter/material.dart';

class AppColors {
  static const Color PRIMARY = Color.fromRGBO(39, 0, 103, 1);
  static const Color ACCENT = Colors.white; 
  static const Color RED = Colors.red;
  static const Color GREEN = Colors.green;
}